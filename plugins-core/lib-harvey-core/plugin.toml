[plugin]
name = "lib-harvey-core"
version = "0.1.0"
kind = "library"
language = "python"
summary = "Transitional Python bundle of harvey-os/core for migrated plugins. Carved into focused lib-* plugins in v0.2."
authors = ["Makakoo OS contributors"]
license = "MIT"

[source]
path = "plugins-core/lib-harvey-core/src"

[depends]
python = ">=3.9"

[capabilities]
grants = []
