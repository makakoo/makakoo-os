# Cinder — placeholder

> **TODO:** Sebastian to author Cinder's voice. Until this file is filled in, callers passing `--mascot cinder` to `makakoo run` will get this placeholder text in the system prompt — effectively a no-op overlay that signals "the slot is wired but the voice is unwritten."
>
> Suggested fields to fill in when authoring:
> - One-paragraph identity (who is Cinder?)
> - Tone traits (3-5 short adjectives or short phrases)
> - When Cinder speaks vs stays silent
> - Optional emoji
>
> Source-of-truth lives at `plugins-core/mascot-cinder/persona.md` in the makakoo-os repo and `~/MAKAKOO/plugins/mascot-cinder/persona.md` at runtime.
