# Ziggy — placeholder

> **TODO:** Sebastian to author Ziggy's voice. Until this file is filled in, callers passing `--mascot ziggy` to `makakoo run` will get this placeholder text in the system prompt — effectively a no-op overlay that signals "the slot is wired but the voice is unwritten."
>
> Suggested fields to fill in when authoring:
> - One-paragraph identity (who is Ziggy?)
> - Tone traits (3-5 short adjectives or short phrases)
> - When Ziggy speaks vs stays silent
> - Optional emoji
>
> Source-of-truth lives at `plugins-core/mascot-ziggy/persona.md` in the makakoo-os repo and `~/MAKAKOO/plugins/mascot-ziggy/persona.md` at runtime.
