[plugin]
name = "skill-ai-ml-multimodal-knowledge"
version = "0.1.0"
kind = "skill"
language = "python"
summary = "multimodal-knowledge skill"
authors = ["Makakoo OS contributors"]
license = "MIT"

[source]
path = "plugins-core/skill-ai-ml-multimodal-knowledge"

[abi]
skill = "^1.0"

[depends]
python = ">=3.8"

[entrypoint]
run = "python3 -u src/ingest.py"

[capabilities]
# Capability grants default empty — conservative for batch migration.
# Add brain/llm/net/state grants manually per skill's real needs.
grants = []

[state]
dir = "$MAKAKOO_HOME/state/skill-ai-ml-multimodal-knowledge"
retention = "keep"
