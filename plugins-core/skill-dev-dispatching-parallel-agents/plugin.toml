[plugin]
name = "skill-dev-dispatching-parallel-agents"
version = "0.1.0"
kind = "skill"
language = "python"
summary = "Use when facing 2+ independent tasks that can be worked on without shared state or sequential dependencies"
authors = ["Makakoo OS contributors"]
license = "MIT"

[source]
path = "plugins-core/skill-dev-dispatching-parallel-agents"

[abi]
skill = "^1.0"

[depends]
python = ">=3.8"

[entrypoint]
run = "python3 -u -m src.__main__"

[capabilities]
# Capability grants default empty — conservative for batch migration.
# Add brain/llm/net/state grants manually per skill's real needs.
grants = []

[state]
dir = "$MAKAKOO_HOME/state/skill-dev-dispatching-parallel-agents"
retention = "keep"
