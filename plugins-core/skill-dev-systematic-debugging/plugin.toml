[plugin]
name = "skill-dev-systematic-debugging"
version = "0.1.0"
kind = "skill"
language = "python"
summary = "Use when encountering any bug, test failure, or unexpected behavior, before proposing fixes"
authors = ["Makakoo OS contributors"]
license = "MIT"

[source]
path = "plugins-core/skill-dev-systematic-debugging"

[abi]
skill = "^1.0"

[depends]
python = ">=3.8"

[entrypoint]
run = "python3 -u -m src.__main__"

[capabilities]
# Capability grants default empty — conservative for batch migration.
# Add brain/llm/net/state grants manually per skill's real needs.
grants = []

[state]
dir = "$MAKAKOO_HOME/state/skill-dev-systematic-debugging"
retention = "keep"
