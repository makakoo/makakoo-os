"""SANCHO handlers bundled with skill-freelance-office (v0.3).

Each handler exposes a ``tick(ctx)`` callable returning a dict for
the SANCHO journal. Importing this package is free — handlers are
lazily-loaded by the kernel.
"""
