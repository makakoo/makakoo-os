[plugin]
name = "skill-infrastructure-orchestrator"
version = "0.1.0"
kind = "skill"
language = "python"
summary = "orchestrator skill"
authors = ["Makakoo OS contributors"]
license = "MIT"

[source]
path = "plugins-core/skill-infrastructure-orchestrator"

[abi]
skill = "^1.0"

[depends]
python = ">=3.8"

[entrypoint]
run = "python3 -u src/message_bus.py"

[capabilities]
# Capability grants default empty — conservative for batch migration.
# Add brain/llm/net/state grants manually per skill's real needs.
grants = []

[state]
dir = "$MAKAKOO_HOME/state/skill-infrastructure-orchestrator"
retention = "keep"
