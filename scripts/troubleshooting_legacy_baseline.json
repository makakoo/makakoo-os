{
  "source_commit": "a455f019d5daf182990db3cca6cf99fd0d56765e",
  "diagnostics": [
    "  skip {}: present on disk without a lock entry — leaving as-is \\\n                     (reconcile: `makakoo plugin uninstall {}` then reinstall)",
    "--allow-risk requires a non-empty --risk-ack explanation",
    "--no-skill-scan is only allowed for local path installs",
    "Skipping security scan for local path install as requested.",
    "[2/3] --skip-daemon — skipping daemon install",
    "[3/3] --skip-infect — skipping bootstrap block infect",
    "agent create '{}': credential check failed: {} (run with --skip-credential-check to scaffold without verifying)",
    "agent slot '{}' has failing transports — fix before `agent start`",
    "agent slot filename '{}' must equal slot_id '{}' (filename and slot_id must match)",
    "agent slot path '{}' has no filename stem",
    "apply_update failed — plugin may be uninstalled: {e}",
    "aws-cli not on PATH; falling back to curl reachability probe \\\n             (auth-fail not distinguishable from endpoint-404 in this mode)",
    "can't determine default distros/ path ({e}). Pass --out <path> explicitly.",
    "cannot back up {} before update — aborting untouched: {e}",
    "cron.schedule '{}' must have 5 space-separated fields (got {})",
    "cron.schedule field {} ('{}') is not a valid cron field (range {}-{})",
    "description must not be empty",
    "destroy cancelled",
    "discord lookup_user query '{query}' is not a numeric snowflake",
    "distro dependency resolution failed before install: {e}",
    "distro install failed — aborting install umbrella",
    "dream failed: {}",
    "duplicate transport.id '{}' in slot — every [[transport]] must have a slot-unique id",
    "email: draft {} not approved (status={:?})",
    "embedding pass failed (continuing): {e}",
    "failed to write migration marker: {e}",
    "grant resolution failed: {e}",
    "installing from unstable git ref `{ref_}` — pass a semver tag or 40-char SHA to pin",
    "linkedin: send refused by policy. Draft {} stays in {:?} status. \\\n             Open LinkedIn directly and paste the approved body. \\\n             (This is not a bug — Makakoo never auto-sends LinkedIn messages.)",
    "makakoo s3 bootstrap: admin API at {ADMIN_API_URL} did not become ready within 30s. \\\n             Is `makakoo plugin start garage-store` running?",
    "makakoo s3 bootstrap: refusing on non-macOS host. \\\n             Garage-store v0.7 is macOS-only. See docs/platform-matrix.md.",
    "no Brain context matched — answering without retrieval",
    "no enabled plugins — pass --include-disabled to save the on-disk set anyway",
    "no legacy data/chat/config.json found — nothing to migrate",
    "no plugins installed — nothing to save. Run `makakoo plugin install` first.",
    "platform not supported — `makakoo agent start {slot_id}` requires macOS launchd or \\\n         Linux systemd-user. Set MAKAKOO_AGENT_SUPERVISOR=foreground to run the supervisor \\\n         directly.",
    "plugin {name} has kind = {} — start/stop/status/restart \\\n                 only supported for service or agent kinds",
    "plugin {name} has no plugins.lock entry — reinstall to create one before toggling",
    "plugin {name} is tarball-sourced — `update` cannot auto-pin a new sha256. Reinstall with `plugin install {} --sha256=<new-hex>` after verifying the upstream hash.",
    "registered manifest is corrupt: {e}; reinstall from source",
    "reinstall failed — plugin is currently uninstalled: {e}",
    "scope.allowed_paths '{}' overlaps with scope.forbidden_paths '{}'",
    "service key exists in Garage but missing from keychain — \\\n                 re-storing from `garage key info <id>`",
    "service plugin {name} has neither [entrypoint].start nor [service].start_cmd",
    "service plugin {name} has no [entrypoint].health or [service].health_endpoint",
    "service plugin {} has neither [entrypoint].stop nor [service].stop_cmd",
    "session has no result handle yet: {}",
    "setup wizard returned non-zero exit code {rc}",
    "slack lookup_user query '{query}' is neither a Slack user_id (U…) nor an email",
    "slot '{}': only process_mode = \"supervised_pair\" is supported in v1",
    "slot_id '{}' contains invalid char '{}'; allowed: a-z A-Z 0-9 - _",
    "slot_id '{}' exceeds 64 chars",
    "slot_id must not be empty",
    "spec file {} has no extension (expected .yaml, .yml, .toml)",
    "status fallback (pgrep) failed: {e}",
    "status fallback (pgrep) failed: {e}; cannot determine state",
    "stop exited {stop_rc} during restart; continuing to start",
    "telegram chat_id '{s}' is not a numeric id",
    "telegram lookup_user user_id '{user_str}' is not numeric",
    "telegram outbound conversation_id '{}' is not a numeric chat_id",
    "telegram: chat id '{s}' is not an integer",
    "telegram: chat id is empty",
    "telegram: draft {} not approved (status={:?}); refusing to send",
    "tool '{}' must be lowercase alphanumeric + underscore (matches mcp__harvey__* convention)",
    "transport '{}' has kind '{}' that doesn't match its config payload (supported: telegram | slack | discord | whatsapp | web | voice_twilio | email)",
    "transport '{}' has no bot-token source (set one of secret_env / secret_ref / inline_secret_dev)",
    "transport '{}' kind=email: account_id (full mailbox address) must not be empty",
    "transport '{}' kind=email: auth_mode must be 'oauth2' or 'app_password', got '{}'",
    "transport '{}' kind=email: both imap_server and smtp_server must be set",
    "transport '{}' kind=email: plain IMAP (143) or SMTP (25) is rejected — use STARTTLS or implicit TLS (993/465/587)",
    "transport '{}' kind=slack: Socket Mode requires an app token (set one of app_token_env / app_token_ref / inline_app_token_dev)",
    "transport '{}' kind=slack: channels list is required when dm_only = false",
    "transport '{}' kind=slack: only mode = \"socket\" is supported in v1",
    "transport '{}' kind=slack: team_id must not be empty",
    "transport '{}' kind=voice_twilio: account_sid must not be empty",
    "transport '{}' kind=voice_twilio: auth_token must be set (locked Q9 — required for X-Twilio-Signature verify + recording basic-auth)",
    "transport '{}' kind=voice_twilio: public_base_url must not be empty (used to build the recording-callback URL Twilio POSTs to)",
    "transport '{}' kind=web: production_mode = true requires \\\n                         a non-empty allowed_origins list (locked Q10)",
    "transport '{}' kind=whatsapp: app_secret must be set (app_secret_env / app_secret_ref / inline_app_secret_dev)",
    "transport '{}' kind=whatsapp: phone_number_id must not be empty",
    "transport '{}' kind=whatsapp: verify_token must be set (verify_token_env / verify_token_ref / inline_verify_token_dev)",
    "triggers[cron].timezone '{}' must be empty, 'UTC', or an IANA timezone (e.g. 'Europe/Berlin')",
    "uninstall step failed: {e}",
    "unsupported extension '.{}' (expected .yaml, .yml, .toml)",
    "update from source_type {other:?} is scheduled for v0.3 Phase E URL fetchers",
    "update requires install.entry_point or install.source; cannot resolve",
    "upstream probe failed for {name}: {e}",
    "venv-bootstrap --mode git requires --url=<repo-url>",
    "venv-bootstrap --mode pip requires --spec=<raw-pip-spec>",
    "venv-bootstrap failed: {e}",
    "venv-bootstrap: $MAKAKOO_PLUGIN_DIR is not set. \\\n                 This command must be invoked from a plugin's [install].unix script.",
    "venv-bootstrap: unknown --mode {other:?} (valid: editable, pip, git)",
    "{slot_id}: launchd bootstrap returned but no status.json in 2s — check \\\n                 {}/data/log/agent-{slot_id}.err.log",
    "{} '{}' must be uppercase env var name (e.g. TELEGRAM_FOO)",
    "{} entry '{}' contains invalid characters (allowed: alphanumeric, _, -)",
    "{} is tarball-sourced; skip (reinstall with new --sha256)"
  ]
}
